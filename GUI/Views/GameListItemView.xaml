<UserControl x:Class="BFGDL.NET.Views.GameListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="90" d:DesignWidth="360">

    <Grid Margin="8,6">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition Width="8" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Thumbnail -->
        <Border Grid.Column="0"
                Width="80" Height="80"
                CornerRadius="4"
                ClipToBounds="True"
                Background="{StaticResource SurfaceHighBrush}">
            <Image Stretch="UniformToFill"
                   Source="{Binding ThumbnailUrl, Converter={StaticResource StringToImageSourceConverter}, ConverterParameter=80}" />
        </Border>

        <!-- Text -->
        <StackPanel Grid.Column="2" VerticalAlignment="Top">
            <TextBlock Text="{Binding Name}"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"
                       MaxHeight="40"
                       TextWrapping="Wrap" />

            <!-- Genre pills -->
            <ItemsControl ItemsSource="{Binding Genres}" Margin="0,4,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource GenrePill}">
                            <TextBlock Text="{Binding}"
                                       FontSize="10"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Release date -->
            <TextBlock Text="{Binding ReleaseDate, StringFormat={}{0:MMM d, yyyy}}"
                       FontSize="11"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,4,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
